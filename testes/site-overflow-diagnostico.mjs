import { createServer } from 'node:http';
import { readFileSync } from 'node:fs';
import { extname, join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
import { chromium } from 'playwright';
const ROOT=dirname(dirname(fileURLToPath(import.meta.url)));const MIME={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.webmanifest':'application/manifest+json'};
const server=createServer((req,res)=>{const raw=(req.url||'/').split('?')[0],name=raw==='/'?'/index.html':raw;try{const body=readFileSync(join(ROOT,decodeURIComponent(name).replace(/^\/+/,'')));res.writeHead(200,{'Content-Type':MIME[extname(name)]||'application/octet-stream'});res.end(body);}catch{res.writeHead(404).end('x')}});await new Promise(r=>server.listen(0,'127.0.0.1',r));const url=`http://127.0.0.1:${server.address().port}/index.html`;
const browser=await chromium.launch();const page=await browser.newPage({viewport:{width:1440,height:1000}});const screens=['registrar','ciclo','grade','estudonovo','leis','cards','extras','links','historico','evolucao','conquistas','desempenhotec','integracaotec','ferramentas','config'];const vps=[['desktop',1440,1000],['notebook',1180,820],['tablet',768,900],['mobile',390,844],['mobile-small',360,740]];
try{await page.goto(url,{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>window.switchScreen&&document.querySelectorAll('.screen').length>=15,{timeout:30000});await page.evaluate(()=>{try{ProfileUI.hideGate()}catch(_){};const g=document.getElementById('profile-gate');if(g)g.style.display='none';const l=document.getElementById('app-loading');if(l)l.style.display='none'});
for(const [vp,w,h] of vps){await page.setViewportSize({width:w,height:h});for(const name of screens){await page.evaluate(n=>switchScreen(n),name);await page.waitForTimeout(80);const rows=await page.evaluate(name=>{const s=document.getElementById('screen-'+name),out=[];const vis=e=>{const c=getComputedStyle(e),r=e.getBoundingClientRect();return c.display!=='none'&&c.visibility!=='hidden'&&r.width>1&&r.height>1};if(s.scrollWidth>s.clientWidth+5)out.push({kind:'screen',delta:s.scrollWidth-s.clientWidth,sw:s.scrollWidth,cw:s.clientWidth});for(const e of [...s.querySelectorAll('*')]){if(!vis(e))continue;const r=e.getBoundingClientRect();if(r.right>innerWidth+6&&!e.closest('.table-scroll,#ciclo-grade,.track-table-wrap,.tec-import-table-wrap,[class*="scroll"],[style*="overflow"]'))out.push({kind:'element',id:e.id||'',class:String(e.className||'').slice(0,120),tag:e.tagName,text:(e.innerText||'').replace(/\s+/g,' ').trim().slice(0,80),left:Math.round(r.left),right:Math.round(r.right),width:Math.round(r.width),vw:innerWidth});}return out;},name);for(const r of rows)console.log('OVERFLOW',JSON.stringify({vp,screen:name,...r}));}}
}finally{await browser.close();await new Promise(r=>server.close(r));}
