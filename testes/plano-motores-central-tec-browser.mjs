import assert from 'node:assert/strict';
import { createServer } from 'node:http';
import { readFileSync } from 'node:fs';
import { extname, join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
import { chromium } from 'playwright';
const ROOT=dirname(dirname(fileURLToPath(import.meta.url))),MIME={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.webmanifest':'application/manifest+json'};
const server=createServer((req,res)=>{const raw=(req.url||'/').split('?')[0],name=raw==='/'?'/index.html':raw;try{const body=readFileSync(join(ROOT,decodeURIComponent(name).replace(/^\/+/,'')));res.writeHead(200,{'Content-Type':MIME[extname(name)]||'application/octet-stream'});res.end(body);}catch{res.writeHead(404).end('nao encontrado');}});await new Promise(r=>server.listen(0,'127.0.0.1',r));
const url=`http://127.0.0.1:${server.address().port}/index.html`,browser=await chromium.launch(),page=await browser.newPage({viewport:{width:390,height:844}}),errors=[];page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error'&&!/net::|ERR_|favicon|Failed to load resource/.test(m.text()))errors.push(m.text());});await page.route(/^https:\/\//,r=>r.abort());const ov=sel=>page.locator(sel).evaluate(el=>Math.max(0,el.scrollWidth-el.clientWidth));
try{
 await page.goto(url,{waitUntil:'commit',timeout:10000});await page.waitForFunction(()=>!!document.body,{timeout:5000});await page.waitForTimeout(1400);
 const boot=await page.evaluate(()=>({central:typeof PlanoMotoresCentralTecV5,g:typeof PlanoMotoresGovernancaV5,s:typeof PlanoSugestoesSimplificadoV3,r:typeof PlanoSugestoesRobustoV8,a:typeof PlanoRobustoAuditV2}));for(const[k,v]of Object.entries(boot))assert.equal(v,'object',`${k} ausente`);
 await page.evaluate(()=>{try{ProfileUI.hideGate();}catch{}const hoje=new Date(),dia=o=>{const d=new Date(hoje);d.setDate(d.getDate()+o);return d.toISOString().slice(0,10);},discs=['Auditoria','Contabilidade','Direito Tributário','AFO'];const rows=discs.flatMap((disc,di)=>Array.from({length:5},(_,ti)=>{const q=30,t=.44+di*.06+ti*.04,ac=Math.round(q*t);return{codigo:`${di+1}.${ti+1}`,nome:`${disc} T${ti+1}`,depth:1,disciplina:disc,questoes:q,acertos:ac,pctAcerto:ac/q*100};}));const snaps=Array.from({length:3},(_,i)=>({id:8800+i,date:dia(-60+i*30),rows}));DB._set(DB.KEYS.tec,snaps);DB.saveExtras([]);const ap=PlanManager.getActivePlan();if(ap)PlanManager.updatePlan(ap.id,{tipo:'Pré-edital',robustoTecPost:null});DesempenhoTecScreen.scopeMode='consolidado';DesempenhoTecScreen.selectedSnapIds=new Set(snaps.map(s=>s.id));DesempenhoTecScreen._scopedC=null;PlanoMotoresGovernancaV5.restaurar();PlanoSugestoesRobustoV8.restaurar();switchScreen('desempenhotec');DesempenhoTecScreen.render();PlanoMotoresCentralTecV5.ensureUi();});
 const tab=page.locator('.tec-subtab[data-tectab="motores"]');assert.equal(await tab.isVisible(),true);await tab.click();await page.waitForSelector('#tec-panel-motores [data-pmc-engine="robusto"]');
 assert.equal(await page.locator('[data-pmc-engine="simplificado"]').count(),1);assert.equal(await page.locator('[data-pmc-engine="robusto"]').count(),1);assert.equal(await page.locator('[data-rv8-panel]').count(),1);assert.equal(await page.locator('[data-rv8]').count(),9);assert.equal(await page.locator('.rv8-pre-note').count(),1);assert.equal(await page.locator('#tec-panel-motores :is([data-rv5-field],[data-rv4-field],.rv5-panel)').count(),0,'Central não pode conter controles antigos');
 assert.equal(await page.locator('[data-pmc-robust-audit]').count(),1);assert.equal(await page.locator('[data-pmc-audit-export]').count(),1);const payload=await page.evaluate(()=>PlanoRobustoAuditV2.gerarPayload());assert.equal(payload.schema,'studynomentor.robusto-audit.v2');assert.match(payload.meta.privacidade,/local-only/);assert.ok(await ov('#tec-panel-motores')<=4);
 const meta=page.locator('[data-rv8="meta"]');await meta.fill('92');await meta.dispatchEvent('change');await page.waitForFunction(()=>PlanoSugestoesRobustoV8.prefs().meta===92);assert.equal(await page.evaluate(()=>PlanoSugestoesRobustoV8.prefs().meta),92);
 // Pós-edital: disciplinas e pesos devem vir dos nomes canônicos do TEC.
 await page.evaluate(()=>{const ap=PlanManager.getActivePlan();PlanManager.updatePlan(ap.id,{tipo:'Pós-edital',robustoTecPost:null});PlanoMotoresCentralTecV5.renderPanel();});await page.waitForSelector('.rv8-post-row');assert.ok(await page.locator('.rv8-post-row').count()>=4);const labels=await page.locator('.rv8-post-row>span').allTextContents();assert(labels.includes('Auditoria')&&labels.includes('Contabilidade'),'lista Pós deve usar nomes do TEC');await page.locator('[data-rv8-post-active]').first().check();await page.locator('[data-rv8-post-weight]').first().fill('2');await page.locator('[data-rv8-post-save]').click();await page.waitForFunction(()=>PlanoSugestoesRobustoV8._postConfig().configurado===true);const pc=await page.evaluate(()=>PlanoSugestoesRobustoV8._postConfig());assert.equal(pc.configurado,true);assert(Object.values(pc.disciplinas).some(x=>x.ativo&&Number(x.peso)===2));
 // Extras executa; não configura.
 await page.evaluate(()=>{const ap=PlanManager.getActivePlan();PlanManager.updatePlan(ap.id,{tipo:'Pré-edital'});switchScreen('extras');ExtrasScreen.selDay=todayLocal();ExtrasScreen.render();});await page.locator('#extras-plano-btn').click();await page.waitForFunction(()=>document.getElementById('ui-modal')?.style.display==='flex',null,{timeout:8000});assert.equal(await page.locator('#ui-modal-body :is([data-rv8],[data-rv8-post-active],[data-pmc-simple],[data-rv5-field],[data-rv4-field])').count(),0);await page.locator('#ui-modal-cancel').click();
 await page.setViewportSize({width:360,height:640});await page.evaluate(()=>{switchScreen('desempenhotec');DesempenhoTecScreen.render();DesempenhoTecScreen.switchTecTab('motores');});assert.ok(await ov('#tec-panel-motores')<=4);assert.deepEqual(errors,[],`erros: ${errors.join(' | ')}`);
 console.log('OK: Central TEC V8 — painéis atuais, Pós por disciplinas TEC, auditoria V2 e Extras sem configuração.');
}finally{await browser.close();await new Promise(r=>server.close(r));}
