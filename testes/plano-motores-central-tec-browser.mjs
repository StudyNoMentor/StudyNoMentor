import assert from 'node:assert/strict';
import { createServer } from 'node:http';
import { readFileSync } from 'node:fs';
import { extname, join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
import { chromium } from 'playwright';
const ROOT=dirname(dirname(fileURLToPath(import.meta.url))),MIME={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8'};
const server=createServer((req,res)=>{const raw=(req.url||'/').split('?')[0],name=raw==='/'?'/index.html':raw;try{const body=readFileSync(join(ROOT,decodeURIComponent(name).replace(/^\/+/,'')));res.writeHead(200,{'Content-Type':MIME[extname(name)]||'application/octet-stream'});res.end(body);}catch{res.writeHead(404).end('nao encontrado');}});await new Promise(r=>server.listen(0,'127.0.0.1',r));
const url=`http://127.0.0.1:${server.address().port}/index.html`,browser=await chromium.launch(),page=await browser.newPage({viewport:{width:390,height:844}}),errors=[];page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error'&&!/net::|ERR_|favicon/.test(m.text()))errors.push(m.text());});const overflow=sel=>page.locator(sel).evaluate(el=>Math.max(0,el.scrollWidth-el.clientWidth));
try{
 await page.goto(url,{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>typeof switchScreen==='function'&&typeof PlanoMotoresCentralTecV2==='object'&&typeof PlanoMotoresGovernancaV5==='object'&&typeof PlanoSugestoesV3==='object'&&typeof PlanoSugestoesSimplificadoV2==='object'&&typeof PlanoSugestoesRobustoV5==='object'&&typeof PlanoRobustoConfigV5==='object',{timeout:30000});
 await page.evaluate(()=>{try{ProfileUI.hideGate();}catch(e){if(typeof _quiet==='function')_quiet(e,'central-tec-gate');}const hoje=new Date(),dia=off=>{const d=new Date(hoje);d.setDate(d.getDate()+off);return d.toISOString().slice(0,10);},discs=['Auditoria','Contabilidade','Direito Tributário','AFO'];DB.saveSubjects(discs.map((nome,i)=>({id:`central-${i}`,nome,ativo:true,peso:1,qtdQuestoes:20-i,pontosPorQuestao:1,minimoPct:50})));const rows=discs.flatMap((disc,di)=>Array.from({length:8},(_,ti)=>{const q=30,taxa=.42+di*.06+ti*.025,ac=Math.round(q*taxa);return{codigo:`${di+1}.${ti+1}`,nome:`${disc} Tópico ${ti+1}`,depth:1,disciplina:disc,questoes:q,acertos:ac,pctAcerto:ac/q*100};}));const snaps=Array.from({length:3},(_,i)=>({id:8800+i,startDate:dia(-60+i*30),endDate:dia(-60+i*30),date:dia(-60+i*30),label:`Central ${i+1}`,rows}));DB._set(DB.KEYS.tec,snaps);DB.saveExtras([]);PlanoEngine.salvarPrefs({...PlanoEngine.DEFAULTS,migracao:4,minAmostra:20,limite:100,sugestoesDisciplinas:3,sugestoesTopicosDisc:1});DesempenhoTecScreen.scopeMode='consolidado';DesempenhoTecScreen.selectedSnapIds=new Set(snaps.map(s=>s.id));DesempenhoTecScreen.rangeStart=null;DesempenhoTecScreen.rangeEnd=null;DesempenhoTecScreen._scopedC=null;DesempenhoTecScreen._planoRefC=null;PlanoEngine._agrC=null;PlanoEngine._tecScopeSignature=null;PlanoEngine._indiceC=new WeakMap();PlanoMotoresGovernancaV5.restaurar();PlanoSugestoesV3.salvar({modo:'robusto'});});
 // A central nasce pelo render normal e é imune à antiga preferência ux-off.
 await page.evaluate(()=>{switchScreen('desempenhotec');DesempenhoTecScreen.render();});await page.waitForSelector('.tec-subtab[data-tectab="motores"]');const tab=page.locator('.tec-subtab[data-tectab="motores"]');assert.equal(await tab.isVisible(),true);assert.equal(await tab.evaluate(el=>el.classList.contains('ux-off')),false,'governança deve neutralizar ux-off na central');await tab.click();await page.waitForSelector('#tec-panel-motores .pmc-engine-card');
 assert.equal(await page.locator('[data-pmc-engine="simplificado"]').count(),1);assert.equal(await page.locator('[data-pmc-engine="robusto"]').count(),1);assert.equal(await page.locator('[data-pmc-simple="meta"]').count(),1);assert.ok(await page.locator('[data-rv5-field]').count()>60,'painel Robusto V5 deve expor política avançada completa');assert.equal(await page.locator('.rv5-section').count(),11,'Robusto deve apresentar 11 grupos de política');assert.equal(await page.locator('[data-rv5-mode]').count(),5,'cinco tipos de ataque devem ser editáveis');assert.equal(await page.locator('[data-rv5-manual]').count(),1);assert.equal(await page.locator('[data-rv5-export]').count(),1);assert.ok((await overflow('#tec-panel-motores'))<=4);
 // Simplificado persiste apenas na própria fonte.
 const meta=page.locator('[data-pmc-simple="meta"]');await meta.fill('93');await meta.dispatchEvent('change');assert.equal(await page.evaluate(()=>PlanoSugestoesSimplificadoV2.prefs().meta),93);
 // Robusto persiste no próprio V5, busca parâmetros, marca alterações e restaura.
 let opt=page.locator('[data-rv5-field="recursos.otimizadorAtivo"]');await opt.uncheck();await page.waitForFunction(()=>PlanoRobustoConfigV5.prefs(PlanoRobustoConfigV5.detectarModo()).recursos.otimizadorAtivo===false);assert.equal(await page.locator('[data-rv5-row="recursos.otimizadorAtivo"]').evaluate(el=>el.classList.contains('is-modified')),true);
 const search=page.locator('[data-rv5-search-input]');await search.fill('meta competitiva');assert.equal(await page.locator('[data-rv5-row]:visible').count(),1,'busca deve reduzir o painel ao parâmetro correspondente');await search.fill('');
 await page.locator('[data-rv5-only-mod]').check();assert.ok(await page.locator('[data-rv5-row]:visible').count()>=1);await page.locator('[data-rv5-only-mod]').uncheck();
 await page.locator('[data-rv5-reset-all]').click();await page.waitForFunction(()=>PlanoRobustoConfigV5.prefs(PlanoRobustoConfigV5.detectarModo()).recursos.otimizadorAtivo===true);
 const manual=await page.evaluate(()=>PlanoRobustoConfigV5.manualHtml(PlanoRobustoConfigV5.detectarModo()));assert.match(manual,/Manual do Módulo Robusto V5/);assert.match(manual,/Imprimir \/ Salvar em PDF/);
 // Extras apenas decide/executa: zero parâmetros de ambos os motores.
 await page.evaluate(()=>{switchScreen('extras');ExtrasScreen.selDay=todayLocal();ExtrasScreen.render();});await page.locator('#extras-plano-btn').click();await page.waitForFunction(()=>document.getElementById('ui-modal')?.style.display==='flex'&&!!document.querySelector('.ps-engine-chooser,.ps-single-engine'),null,{timeout:8000});assert.equal(await page.locator('#ui-modal-body [data-pmc-simple],[data-rv5-field],[data-rv5-reset-all],[data-rv4-field],[data-ps-meta],[data-ps-min],[data-ps-alvo],[data-ps-banca],[data-ps-fase]').count(),0,'Extras não pode editar parâmetro de motor');assert.match(await page.locator('#ui-modal-body').textContent(),/Desempenho TEC.*Motores|Configuração centralizada/i);await page.locator('#ui-modal-cancel').click();
 // Governança por perfil/estado: mostra apenas motor ativo; zero motores remove a central.
 await page.evaluate(()=>PlanoMotoresGovernancaV5.salvar({robusto:false,simplificado:true}));await page.evaluate(()=>{switchScreen('desempenhotec');DesempenhoTecScreen.render();DesempenhoTecScreen.switchTecTab('motores');});assert.equal(await tab.isVisible(),true);assert.equal(await tab.evaluate(el=>el.classList.contains('ux-off')),false);assert.equal(await page.locator('[data-pmc-engine="simplificado"]').count(),1);assert.equal(await page.locator('[data-pmc-engine="robusto"]').count(),0);
 await page.evaluate(()=>PlanoMotoresGovernancaV5.salvar({robusto:true,simplificado:false}));await page.evaluate(()=>{DesempenhoTecScreen.render();DesempenhoTecScreen.switchTecTab('motores');});assert.equal(await page.locator('[data-pmc-engine="simplificado"]').count(),0);assert.equal(await page.locator('[data-pmc-engine="robusto"]').count(),1);
 await page.evaluate(()=>PlanoMotoresGovernancaV5.salvar({robusto:false,simplificado:false}));await page.evaluate(()=>DesempenhoTecScreen.render());assert.equal(await tab.isVisible(),false);assert.notEqual(await page.evaluate(()=>DesempenhoTecScreen.tecTab),'motores');await page.evaluate(()=>PlanoMotoresGovernancaV5.restaurar());
 await page.setViewportSize({width:360,height:640});await page.evaluate(()=>{switchScreen('desempenhotec');DesempenhoTecScreen.render();DesempenhoTecScreen.switchTecTab('motores');});assert.ok((await overflow('#tec-panel-motores'))<=4,'painel especialista deve caber em 360px');assert.deepEqual(errors,[],`erros no navegador: ${errors.join(' | ')}`);console.log('OK: central TEC V2 — Simplificado e Robusto V5 configurados apenas no TEC, painel especialista completo e Extras sem parâmetros.');
}finally{await browser.close();await new Promise(r=>server.close(r));}